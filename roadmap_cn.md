# 路线图

本文档概述了 Serena 正在进行和未来的开发。如果您有提案或想讨论一些事情，请随时在 Github 上提出讨论。有关过去开发的摘要，请参阅[更新日志](/CHANGELOG.md)。

想看到我们更快地实现目标吗？您可以通过提交问题、开始讨论或告知我们资助机会来提供帮助，以便我们可以投入更多时间到项目中。

## 总体目标

Serena 有潜力成为大多数 LLM 编码任务的首选工具，因为它能够以 MCP 服务器的身份在任何环境中独一无二地使用，同时仍然是一个强大的代理。我们希望在功能方面实现以下目标：

1.  通过官方（免费）客户端（如 Claude Desktop）使用时，性能卓越（与基于 API 的编码代理媲美）。
2.  降低 API 成本，并可能提高编码客户端（Claude Code、Codex、Cline、Roo、Cursor/Windsurf/VSCode 等）的性能。
3.  透明度和易用性。通过仪表板/日志 GUI 实现。
4.  与不接受 MCP 的主要框架集成。可用作库。

除了功能目标之外，我们的目标是拥有出色的代码设计，以便 Serena 可以被视为 MCP 服务器实现的参考。此类项目是一种新兴技术，最佳实践尚待确定。我们将在[经验总结](/lessons_learned.md)中分享我们的经验。


## 立即/正在进行

-   支持使用多种编程语言的项目。
-   评估是否可以删除 `ReplaceLinesTool` 以支持更可靠、性能更好的编辑方法。
-   通常尝试各种编辑工具方法。
-   对 SWE-verified 中选定任务进行手动评估。
-   手动评估在流行的非 MCP 代理中使用时的成本降低和性能。
-   提示改进，特别是提供示例并扩展模式和上下文。

## 即将推出

-   将 Serena 发布为一个可以作为库使用的包。
-   在工具中使用 LSP 的 linting 和类型层次结构。
-   重构工具（重命名、移动）——推测性，可能不会做。
-   使用仪表板跟踪编辑并回滚它们。
-   改进 shell 工具的可配置性和安全性。也许可以从命令和描述列表中自动生成工具。
-   与 DesktopCommander 等进行透明比较。
-   使用 OpenHands 进行自动评估，提交到 SWE-Bench。
-   评估是否合并其他 MCP 会提高性能或可用性（内存银行是一个候选）。
-   更多文档和最佳实践。

## 延伸

-   允许沙箱化和并行 Serena 实例，也许可以使用 openhands 或 codex。
-   合并一个验证器模型或通常是第二个模型（可能用于应用编辑）作为工具。
-   在此基础上，允许第二个模型本身可以通过 MCP 服务器访问，这样它就可以免费使用。
-   跟踪使用 shell 工具执行的编辑。

## 超越 Serena

Serena 中采用的技术和方法可用于各种研究和服务想法。我们的一些想法包括：

-   与 GitHub 合作的 PR 和问题助手，类似于 [OpenHands](https://github.com/All-Hands-AI/OpenHands) 和 [qodo](https://github.com/qodo-ai/pr-agent) 的操作方式。应该可以通过 @serena 调用。
-   使用 Serena 的工具对 LLM 编码模型进行 RL 微调，用于一次性任务。为此我们需要计算资金。
-   开发一个 Web 应用程序，通过抓取 PR 和手动创建的元数据来定量比较各种代理的性能。
    编码代理的主要指标应该是**开发者体验**，而这很难把握，并且与当前基准的性能相关性很低。 